[project]
name = "aws-llm-league"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain>=0.3.21",
    "langgraph-cli[inmem]>=0.1.79",
    "langgraph>=0.3.18",
]
